//a function that receives 2 numbers and returns the smallest one

let number1 = 2;
let number2 = -2;

const minimumFunction = (num1, num2) => {
    return Math.min(num1, num2);
}
console.log(minimumFunction(number1, number2));